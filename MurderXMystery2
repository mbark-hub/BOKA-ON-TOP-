local SupportedPlaceId = 142823291

if game.PlaceId ~= SupportedPlaceId then
    game.Players.LocalPlayer:Kick("الماب غير مدعوم فقط يدعم ماب : MM2")
    return
end

print("تم تشغيل السكربت داخل الماب المدعوم MM2")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera


local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BOKA_HUB_MM2"
ScreenGui.Parent = PlayerGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false


local CurrentLanguage = "Arabic"


local VerifyFrame = Instance.new("Frame")
VerifyFrame.Size = UDim2.new(0, 300, 0, 180)
VerifyFrame.Position = UDim2.new(0.5, -150, 0.5, -90)
VerifyFrame.BackgroundColor3 = Color3.new(0, 0, 0)
VerifyFrame.BorderSizePixel = 0
VerifyFrame.Active = true
VerifyFrame.Visible = true
VerifyFrame.Parent = ScreenGui

local VerifyCorner = Instance.new("UICorner")
VerifyCorner.CornerRadius = UDim.new(0, 10)
VerifyCorner.Parent = VerifyFrame

local VerifyStroke = Instance.new("UIStroke")
VerifyStroke.Thickness = 3
VerifyStroke.Color = Color3.fromRGB(255, 0, 0)
VerifyStroke.Parent = VerifyFrame

task.spawn(function()
    while task.wait() do
        local t = tick() * 2
        VerifyStroke.Color = Color3.fromHSV(0, math.abs(math.sin(t)), 1)
    end
end)


local LangPage = Instance.new("Frame")
LangPage.Size = UDim2.new(1, 0, 1, 0)
LangPage.BackgroundTransparency = 1
LangPage.Parent = VerifyFrame

local VerifyTitle = Instance.new("TextLabel")
VerifyTitle.Size = UDim2.new(1, 0, 0, 50)
VerifyTitle.Text = "التحقق"
VerifyTitle.TextColor3 = Color3.new(1, 1, 1)
VerifyTitle.TextSize = 18
VerifyTitle.Font = Enum.Font.GothamBold
VerifyTitle.BackgroundTransparency = 1
VerifyTitle.Parent = LangPage

local LangText = Instance.new("TextLabel")
LangText.Size = UDim2.new(1, 0, 0, 30)
LangText.Position = UDim2.new(0, 0, 0.3, 0)
LangText.Text = "وش اللغة ؟ / What your language ?"
LangText.TextColor3 = Color3.new(1, 1, 1)
LangText.TextSize = 14
LangText.Font = Enum.Font.GothamMedium
LangText.BackgroundTransparency = 1
LangText.Parent = LangPage

local ArabicBtn = Instance.new("TextButton")
ArabicBtn.Size = UDim2.new(0, 100, 0, 40)
ArabicBtn.Position = UDim2.new(0.2, 0, 0.6, 0)
ArabicBtn.Text = "العربية"
ArabicBtn.TextSize = 28
ArabicBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ArabicBtn.TextColor3 = Color3.new(1, 1, 1)
ArabicBtn.Font = Enum.Font.GothamBold
ArabicBtn.Parent = LangPage
Instance.new("UICorner", ArabicBtn).CornerRadius = UDim.new(0, 6)

local EnglishBtn = Instance.new("TextButton")
EnglishBtn.Size = UDim2.new(0, 100, 0, 40)
EnglishBtn.Position = UDim2.new(0.55, 0, 0.6, 0)
EnglishBtn.Text = "English"
EnglishBtn.TextSize = 28
EnglishBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
EnglishBtn.TextColor3 = Color3.new(1, 1, 1)
EnglishBtn.Font = Enum.Font.GothamBold
EnglishBtn.Parent = LangPage
Instance.new("UICorner", EnglishBtn).CornerRadius = UDim.new(0, 6)


local ConfirmPage = Instance.new("Frame")
ConfirmPage.Size = UDim2.new(1, 0, 1, 0)
ConfirmPage.BackgroundTransparency = 1
ConfirmPage.Visible = false
ConfirmPage.Parent = VerifyFrame

local ConfirmText = Instance.new("TextLabel")
ConfirmText.Size = UDim2.new(1, 0, 0, 50)
ConfirmText.Position = UDim2.new(0, 0, 0.2, 0)
ConfirmText.Text = "هل أنت متأكد ؟"
ConfirmText.TextColor3 = Color3.new(1, 1, 1)
ConfirmText.TextSize = 18
ConfirmText.Font = Enum.Font.GothamBold
ConfirmText.BackgroundTransparency = 1
ConfirmText.Parent = ConfirmPage

local YesBtn = Instance.new("TextButton")
YesBtn.Size = UDim2.new(0, 100, 0, 40)
YesBtn.Position = UDim2.new(0.2, 0, 0.6, 0)
YesBtn.Text = "Yes"
YesBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
YesBtn.TextColor3 = Color3.new(1, 1, 1)
YesBtn.Font = Enum.Font.GothamBold
YesBtn.Parent = ConfirmPage
Instance.new("UICorner", YesBtn).CornerRadius = UDim.new(0, 6)

local NoBtn = Instance.new("TextButton")
NoBtn.Size = UDim2.new(0, 100, 0, 40)
NoBtn.Position = UDim2.new(0.55, 0, 0.6, 0)
NoBtn.Text = "No"
NoBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
NoBtn.TextColor3 = Color3.new(1, 1, 1)
NoBtn.Font = Enum.Font.GothamBold
NoBtn.Parent = ConfirmPage
Instance.new("UICorner", NoBtn).CornerRadius = UDim.new(0, 6)


local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 300, 0, 300)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.new(0, 0, 0)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 3
UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
UIStroke.Parent = MainFrame

task.spawn(function()
    while task.wait() do
        local t = tick() * 2
        local color = Color3.fromHSV(0, math.abs(math.sin(t)), 1)
        UIStroke.Color = color
    end
end)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 35)
Title.BackgroundTransparency = 1
Title.Text = "Murder Mystery 2 | BOKA ON TOP "
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextSize = 14
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame


local ActivateBtn = Instance.new("TextButton")
ActivateBtn.Size = UDim2.new(0, 240, 0, 45) 
ActivateBtn.Position = UDim2.new(0.5, -120, 0.15, 0)
ActivateBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ActivateBtn.Text = "🔴 تفعيل"
ActivateBtn.TextColor3 = Color3.new(1, 1, 1)
ActivateBtn.TextSize = 18
ActivateBtn.Font = Enum.Font.GothamBold
ActivateBtn.Parent = MainFrame
Instance.new("UICorner", ActivateBtn).CornerRadius = UDim.new(0, 8)


local AimbotBtn = Instance.new("TextButton")
AimbotBtn.Size = UDim2.new(0, 240, 0, 45)
AimbotBtn.Position = UDim2.new(0.5, -120, 0.35, 0)
AimbotBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
AimbotBtn.Text = "🔴 ايم بوت للقاتل"
AimbotBtn.TextColor3 = Color3.new(1, 1, 1)
AimbotBtn.TextSize = 18
AimbotBtn.Font = Enum.Font.GothamBold
AimbotBtn.Parent = MainFrame
Instance.new("UICorner", AimbotBtn).CornerRadius = UDim.new(0, 8)


local ProtectSheriffBtn = Instance.new("TextButton")
ProtectSheriffBtn.Size = UDim2.new(0, 240, 0, 45)
ProtectSheriffBtn.Position = UDim2.new(0.5, -120, 0.55, 0)
ProtectSheriffBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ProtectSheriffBtn.Text = "🔴 حماية الشرف"
ProtectSheriffBtn.TextColor3 = Color3.new(1, 1, 1)
ProtectSheriffBtn.TextSize = 18
ProtectSheriffBtn.Font = Enum.Font.GothamBold
ProtectSheriffBtn.Parent = MainFrame
Instance.new("UICorner", ProtectSheriffBtn).CornerRadius = UDim.new(0, 8)

local Note = Instance.new("TextLabel")
Note.Size = UDim2.new(1, 0, 0, 20)
Note.Position = UDim2.new(0, 0, 0.9, 0)
Note.BackgroundTransparency = 1
Note.Text = "ملاحظة: ذا السكربت يدعم ماب MM2 وفي اشياء ثانيه شوفها بنفسك!"
Note.Font = Enum.Font.GothamMedium
Note.TextSize = 9
Note.Parent = MainFrame

task.spawn(function()
    while task.wait(0.5) do
        Note.TextColor3 = Color3.new(1, 0, 0)
        task.wait(0.5)
        Note.TextColor3 = Color3.new(1, 1, 1)
    end
end)


local function PlayNotify(title, text)
    local sound = Instance.new("Sound")
    sound.SoundId = "rbxassetid://170765130"
    sound.Parent = game.Workspace
    sound:Play()
    
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = title,
        Text = text,
        Icon = "rbxthumb://type=Asset&w=420&h=420&id=110338338499727",
        Duration = 5
    })
end

local function CreateESP(player, color, roleName)
    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local highlight = player.Character:FindFirstChild("BOKA_ESP")
        if not highlight then
            highlight = Instance.new("Highlight")
            highlight.Name = "BOKA_ESP"
            highlight.OutlineColor = Color3.new(1, 1, 1)
            highlight.FillTransparency = 0.5
            highlight.Parent = player.Character
        end
        highlight.Adornee = player.Character
        highlight.FillColor = color

        local billboard = player.Character:FindFirstChild("RoleTag")
        if not billboard then
            billboard = Instance.new("BillboardGui")
            billboard.Name = "RoleTag"
            billboard.Size = UDim2.new(0, 100, 0, 50)
            billboard.AlwaysOnTop = true
            billboard.ExtentsOffset = Vector3.new(0, 3, 0)
            
            local label = Instance.new("TextLabel")
            label.Name = "Label"
            label.Size = UDim2.new(1, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.TextStrokeTransparency = 0
            label.Font = Enum.Font.GothamBold
            label.TextSize = 14
            label.Parent = billboard
            
            billboard.Parent = player.Character
        end
        billboard.Adornee = player.Character:WaitForChild("Head")
        billboard.Label.Text = roleName
        billboard.Label.TextColor3 = color
    end
end

local function RemoveESP()
    for _, v in pairs(Players:GetPlayers()) do
        if v.Character then
            if v.Character:FindFirstChild("BOKA_ESP") then v.Character.BOKA_ESP:Destroy() end
            if v.Character:FindFirstChild("RoleTag") then v.Character.RoleTag:Destroy() end
        end
    end
end

local function ScanPlayers()
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character then
            local roleColor = Color3.new(0, 1, 0)
            local roleName = (CurrentLanguage == "Arabic") and "بريئ" or "INNOCENT"
            
            if v.Backpack:FindFirstChild("Knife") or v.Character:FindFirstChild("Knife") then
                roleColor = Color3.new(1, 0, 0)
                roleName = (CurrentLanguage == "Arabic") and "قاتل" or "MURDER"
            elseif v.Backpack:FindFirstChild("Gun") or v.Character:FindFirstChild("Gun") then
                roleColor = Color3.new(0, 0, 1)
                roleName = (CurrentLanguage == "Arabic") and "شرف" or "SHERIFF"
            end
            CreateESP(v, roleColor, roleName)
        end
    end
end


local AimbotToggled = false
local function GetMurderer()
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            if v.Backpack:FindFirstChild("Knife") or v.Character:FindFirstChild("Knife") then
                return v.Character.HumanoidRootPart
            end
        end
    end
    return nil
end


local function GetSheriff()
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            if v.Backpack:FindFirstChild("Gun") or v.Character:FindFirstChild("Gun") then
                return v.Character
            end
        end
    end
    return nil
end

local ProtectToggled = false
RunService.RenderStepped:Connect(function()

    if AimbotToggled then
        if UserInputService.MouseBehavior == Enum.MouseBehavior.LockCenter then
            local target = GetMurderer()
            if target then
                Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Position)
            end
        end
    end
    

    if ProtectToggled then
        local sheriffChar = GetSheriff()
        if sheriffChar and sheriffChar:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local root = LocalPlayer.Character.HumanoidRootPart
            local sheriffRoot = sheriffChar.HumanoidRootPart
           
            root.CFrame = sheriffRoot.CFrame * CFrame.new(0, 0, 1)
        end
    end
end)


ArabicBtn.MouseButton1Click:Connect(function()
    CurrentLanguage = "Arabic"
    ConfirmText.Text = "هل أنت متأكد ؟"
    YesBtn.Text = "نعم"
    NoBtn.Text = "لا"
    LangPage.Visible = false
    ConfirmPage.Visible = true
end)

EnglishBtn.MouseButton1Click:Connect(function()
    CurrentLanguage = "English"
    ConfirmText.Text = "Are you sure?"
    YesBtn.Text = "Yes"
    NoBtn.Text = "No"
    LangPage.Visible = false
    ConfirmPage.Visible = true
end)

NoBtn.MouseButton1Click:Connect(function()
    ConfirmPage.Visible = false
    LangPage.Visible = true
end)

YesBtn.MouseButton1Click:Connect(function()
    VerifyFrame.Visible = false
    MainFrame.Visible = true
    
    if CurrentLanguage == "Arabic" then
        ActivateBtn.Text = "🔴 تفعيل"
        AimbotBtn.Text = "🔴 ايم بوت للقاتل"
        ProtectSheriffBtn.Text = "🔴 حماية الشرف"
        Note.Text = "ملاحظة: ذا السكربت يدعم ماب MM2 وفي اشياء ثانيه شوفها بنفسك!"
        PlayNotify("صاحب السكربت", "منور السكربت يا " .. LocalPlayer.DisplayName)
    else
        ActivateBtn.Text = "🔴 Activate"
        AimbotBtn.Text = "🔴 Murderer Aimbot"
        ProtectSheriffBtn.Text = "🔴 Protect Sheriff"
        Note.Text = "This script supports MM2 maps and other features; see for yourself!"
        PlayNotify("Owner script", "Welcome to the script, " .. LocalPlayer.DisplayName)
    end
end)


local Toggled = false
local UpdateLoop
ActivateBtn.MouseButton1Click:Connect(function()
    Toggled = not Toggled
    if Toggled then
        ActivateBtn.Text = (CurrentLanguage == "Arabic") and "🟢 تفعيل" or "🟢 Enabled"
        ActivateBtn.TextColor3 = Color3.new(0, 1, 0)
        UpdateLoop = task.spawn(function()
            while Toggled do ScanPlayers() task.wait(1) end
        end)
    else
        ActivateBtn.Text = (CurrentLanguage == "Arabic") and "🔴 تفعيل" or "🔴 Activate"
        ActivateBtn.TextColor3 = Color3.new(1, 1, 1)
        Toggled = false
        if UpdateLoop then task.cancel(UpdateLoop) end
        RemoveESP()
    end
end)


AimbotBtn.MouseButton1Click:Connect(function()
    if UserInputService.MouseBehavior ~= Enum.MouseBehavior.LockCenter then
        local msg = (CurrentLanguage == "Arabic") and "ما يمديك تفعل الزر لازم تفعل قفل الكاميرا" or "You must enable Shift Lock to use this"
        local title = (CurrentLanguage == "Arabic") and "صاحب السكربت" or "Owner script"
        PlayNotify(title, msg)
        return
    end

    AimbotToggled = not AimbotToggled
    if AimbotToggled then
        AimbotBtn.Text = (CurrentLanguage == "Arabic") and "🟢 ايم بوت للقاتل" or "🟢 Murderer Aimbot"
        AimbotBtn.TextColor3 = Color3.new(0, 1, 0)
    else
        AimbotBtn.Text = (CurrentLanguage == "Arabic") and "🔴 ايم بوت للقاتل" or "🔴 Murderer Aimbot"
        AimbotBtn.TextColor3 = Color3.new(1, 1, 1)
    end
end)


ProtectSheriffBtn.MouseButton1Click:Connect(function()

    local sheriffExists = GetSheriff()
    
    if not ProtectToggled then 
        if not sheriffExists then

            local msg = (CurrentLanguage == "Arabic") and "الشرف مو موجود" or "Sheriff is not there"
            local title = (CurrentLanguage == "Arabic") and "صاحب السكربت" or "Owner script"
            PlayNotify(title, msg)
            return
        end
    end

    ProtectToggled = not ProtectToggled
    if ProtectToggled then
        ProtectSheriffBtn.Text = (CurrentLanguage == "Arabic") and "🟢 حماية الشرف" or "🟢 Protect Sheriff"
        ProtectSheriffBtn.TextColor3 = Color3.new(0, 1, 0)
        PlayNotify("BOKA", (CurrentLanguage == "Arabic") and "تم تفعيل حماية الشرف" or "Sheriff Protect Enabled")
    else
        ProtectSheriffBtn.Text = (CurrentLanguage == "Arabic") and "🔴 حماية الشرف" or "🔴 Protect Sheriff"
        ProtectSheriffBtn.TextColor3 = Color3.new(1, 1, 1)
    end
end)
